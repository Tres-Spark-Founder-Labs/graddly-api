# k6 smoke sign-off — 2026-06-08

**Environment:** Staging (ops-owned) · **Script:** [`../smoke.js`](../smoke.js)

## Procedure

```bash
export BASE_URL=https://staging-api.graddly.com
export TEST_EMAIL=load-test@example.com
export TEST_PASSWORD='...'
export ORG_ID='...'
yarn load:smoke
```

Local alternative (API + worker + Postgres + Redis on port 3010):

```bash
yarn load:smoke:local
```

## PRD §7.1 thresholds

| Threshold | Target |
|-----------|--------|
| `http_req_duration{scenario:read}` p95 | &lt; 500ms |
| `http_req_failed` rate | &lt; 1% |

## Scenarios exercised

| Endpoint | Method | Auth |
|----------|--------|------|
| `/api/v1/otj-log-entries` | GET | JWT + optional org header |
| `/api/v1/notifications` | GET | JWT (unread filter) |

## Sign-off status

| Check | Status | Notes |
|-------|--------|-------|
| Script + thresholds defined | Pass | [`smoke.js`](../smoke.js), [`performance.md`](../../docs/performance.md) |
| Index migration applied | Pass | `1780800000000-PerfIndexReview` |
| Staging execution recorded | **Pending ops** | Run before production release; paste k6 summary below |

### Staging run (fill after ops execution)

```
Date:
BASE_URL:
p95 read (ms):
http_req_failed rate:
Pass/Fail:
Operator:
```

## Local validation (2026-06-08)

`yarn load:smoke:local` was attempted in CI/dev; k6 is installed (`/opt/homebrew/bin/k6`). Login failed because no API was listening on `localhost:3010` — expected without a running stack. Re-run with `yarn start` (or point `BASE_URL` at staging) for a full pass.
