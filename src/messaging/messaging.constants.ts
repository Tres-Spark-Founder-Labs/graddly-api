/** Maximum attachment size for messaging (10 MB per PRD F3.4.2). */
export const MAX_MESSAGE_ATTACHMENT_BYTES = 10 * 1024 * 1024;

/** Debounced email notification delay (5 minutes per PRD). */
export const MESSAGE_EMAIL_DEBOUNCE_MS = 300_000;

/** Maximum message body length. */
export const MAX_MESSAGE_BODY_LENGTH = 4000;
